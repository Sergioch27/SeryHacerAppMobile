import { StyleSheet, Text, SafeAreaView } from 'react-native'
import React from 'react'

const PodcastView = () => {
  return (
    <SafeAreaView>
      <Text>PodcastView</Text>
    </SafeAreaView>
  )
}

export default PodcastView

const styles = StyleSheet.create({})